import { Button } from "@/components/ui/button";
import { Check, Clock } from "lucide-react";

const FloatingOffer = () => {
  return (
    <div className="fixed bottom-20 left-4 right-4 sm:bottom-6 sm:right-6 sm:left-auto z-50 hidden lg:block">
      {/* FloatingOffer component can be empty or removed since it's not showing any content */}
    </div>
  );
};

export default FloatingOffer;